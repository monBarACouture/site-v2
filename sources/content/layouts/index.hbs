<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		{{> head/title}}
		{{> head/styles}}
		{{> head/scripts}}
	</head>
	<body>
		{{> menu}}
		{{#if (existy page)}}
			{{> (lookup . 'page')}}
		{{else}}
			{{{contents}}}
		{{/if}}
	</body>
</html>
